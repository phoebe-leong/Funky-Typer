window.onload = () => {
	$("textarea").inputfit()

	/*if ($("textarea").scrollWidth > $("textarea").innerWidth || $("textarea").scrollHeight > $("textarea").innerHeight) {
		console.log("overflown!")
	}

	if ($("textarea").scrollWidth > $(window).width() || $("textarea").scrollHeight > $(window).height()) {
		console.log("overflow")
	}*/
}